apiVersion: v1
kind: Service
metadata:
  name: mlops-serving
  namespace: mlops
spec:
  selector:
    app: mlops-serving
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
